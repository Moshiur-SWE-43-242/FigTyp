# Backend API origin used by the frontend.
# Leave unset in development (defaults to http://localhost:5000).
# For production builds (e.g. Firebase Hosting), set this to your deployed backend URL
# BEFORE running `npm run build`, e.g.:
# VITE_API_URL=https://figtyp-api.example.com
VITE_API_URL=
